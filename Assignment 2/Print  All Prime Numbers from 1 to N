4. Print All Prime Numbers from 1 to N
Input: A number N
Output: All prime numbers between 1 and N using for loop and if conditions

import java.util.Scanner;
public class PrimeNumbers{
  public static void main(String[] args) {
     Scanners = new Scanner(System.in);
     int N = s.nextInt();
     for (int i = 2; i <= N; i++) {
         boolean prime = true;
         for (int j = 2; j <= i / 2; j++) {
             if (i % j == 0) {
                 prime = false;
                 break;
             }
         }
         if (prime) {
             System.out.print(i + " ");
         }
     }
   }
}
